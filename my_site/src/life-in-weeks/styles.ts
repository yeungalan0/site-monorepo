import { makeStyles } from "@mui/styles";

const liwStyles = makeStyles(() => ({
  birthdateFormStyles: {
    justifyContent: "center",
    alignItems: "center",
    flex: "1",
    display: "flex",
    flexDirection: "column",
    textAlign: "center",
  },
  cellStyles: {
    padding: "0",
  },
}));

export { liwStyles };
